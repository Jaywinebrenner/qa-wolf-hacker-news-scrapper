{
    "version": 2,
    "builds": [
      {
        "src": "index.js",
        "use": "@vercel/node",
        "config": {
          "buildCommand": "npx playwright install chromium"
        }
      }
    ],
    "routes": [
      { "src": "/api/articles", "dest": "/index.js" },
      { "src": "/(.*)", "dest": "/index.js" }
    ]
  }
  